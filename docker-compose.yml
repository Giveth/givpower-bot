version: '3.9'

services:
  givpower-bot:
    image: ghcr.io/giveth/givpower-bot:main
    environment:
      - NODE_ENV=develop
    restart: unless-stopped
    volumes:
      - ./config:/usr/src/app/config
    ports:
      - "3002:3000"
